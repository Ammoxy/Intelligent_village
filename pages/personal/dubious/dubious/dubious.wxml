<block wx:for="{{dangerFaceList}}" wx:key>
	<view class="renter">
		<view bindtap="toDubiousInfo">{{item.name}}</view>
		<view bindtap="toCapture" data-id="{{item.danger_id}}">查看抓拍记录</view>
	</view>
</block>