<!--pages/personal/dubious-face/dubious-face/dubious-face.wxml-->
<view class="box">
	<!-- <view wx:for="{{renterList}}" wx:key="{{index}}" class="list">
		<view class="info-list">
			<view data-name="{{item.snapshot.name}}" data-sex="{{item.snapshot.sex}}" data-card_number="{{item.snapshot.card_number}}" data-href="{{item.snapshot.href}}" data-address="{{item.address.address}}" data-phone="{{item.snapshot.phone}}" bindtap="renterDetail">
				<image src="/icon/children.png"></image>
				<text class="guardian">{{item.snapshot.name}}</text>
			</view>
			<view>
				<image src="/icon/unit.png"></image>
				<text>{{item.room.door_number}}</text>
			</view>
		</view>
		<button>详情</button>
	</view> -->
	<view class="search">
		<picker mode="date" start="2020-01-01" end="2030-01-01" bindchange="bindBegin">
			<input placeholder="开始时间" disabled="true" value="{{dateBegin}}"></input>
		</picker>
		<view>至</view>
		<picker mode="date" start="2020-01-01" end="2030-01-01" bindchange="bindEnd">
			<input placeholder="结束时间" disabled="true" value="{{dateEnd}}"></input>
		</picker>
		<view class="img_box" bindtap="search">
			<icon class="icon-small" type="search" size="15"></icon>
		</view>
	</view>
	<view class="header">
		<!-- <text class="header_img">单元</text> -->
		<text class="header_box">地址</text>
		<text class="header_btn">操作</text>
	</view>
	<view class="top"></view>
	<view class="list">
		<view class="info-list">
			<!-- <view class="list_img">
				<image src="/icon/unit.png"></image>
			</view> -->
			<view class="list_box">龙瑞大街40号龙瑞大街40号龙瑞大街40号龙瑞大街40号龙瑞大街40号龙瑞大街40号</view>
			<view class="list_btn">
				<button class="btn" type="primary" size="mini" bindtap="toRecord">抓拍记录</button>
				<!-- <button class="mini-btn" type="warn" size="mini" bindtap="delete">删除</button> -->
			</view>
		</view>
	</view>
	<view class="list">
		<view class="info-list">
			<!-- <view class="list_img">
				<image src="/icon/unit.png"></image>
			</view> -->
			<view class="list_box">111</view>
			<view class="list_btn">
				<button class="btn" type="primary" size="mini" bindtap="toRecord">抓拍记录</button>
				<!-- <button class="mini-btn" type="warn" size="mini" bindtap="delete">删除</button> -->
			</view>
		</view>
	</view>
</view>