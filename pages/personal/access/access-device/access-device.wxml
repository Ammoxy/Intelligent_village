<block wx:for="{{deviceList}}" wx:key="index">
	<view class="renter">
		<view bindtap="toDeviceInfo" data-uuid="{{item.uuid}}">{{item.uuid}}</view>
		<view bindtap="toInOutLogs" data-addressid="{{item.address_id}}">查看进出/抓拍记录</view>
	</view>
</block>